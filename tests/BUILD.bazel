# =============================================================================
# TIANSHU tests Bazel build (top-level)
# =============================================================================

load("@rules_cc//cc:defs.bzl", "cc_test")

package(default_visibility = ["//visibility:public"])

cc_test(
    name = "hello_test",
    size = "small",
    srcs = ["hello_test.cc"],
    deps = [
        "//tianshu",
        "@googletest//:gtest_main",
    ],
)

# Subdirectories have their own BUILD.bazel files.
# Bazel discovers them automatically via //...
